{
  "name": "cron-poller",
  "version": "0.1.0",
  "description": "Periodically polls a target URL using Spin cron jobs",
  "type": "module",
  "scripts": {
    "build": "node build.mjs && mkdirp dist && j2w -i build/bundle.js --initLocation http://cron-poller.localhost -o dist/cron-poller.wasm"
  },
  "dependencies": {
    "itty-router": "^5.0.18",
    "@spinframework/wasi-http-proxy": "^1.0.0"
  },
  "devDependencies": {
    "@spinframework/build-tools": "^1.0.4",
    "esbuild": "^0.25.8",
    "mkdirp": "^3.0.1"
  }
}
