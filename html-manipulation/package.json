{
  "name": "html-rewriter",
  "version": "0.1.0",
  "description": "Rewrites HTML relative links to absolute URLs",
  "type": "module",
  "scripts": {
    "build": "node build.mjs && mkdirp dist && j2w -i build/bundle.js --initLocation http://html-rewriter.localhost -o dist/html-rewriter.wasm"
  },
  "dependencies": {
    "@spinframework/wasi-http-proxy": "^1.0.0"
  },
  "devDependencies": {
    "@spinframework/build-tools": "^1.0.4",
    "esbuild": "^0.25.8",
    "mkdirp": "^3.0.1"
  }
}
